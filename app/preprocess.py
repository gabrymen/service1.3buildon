import pandas as pd

def preprocess_data(data):
    #df = pd.DataFrame(data)
    #df = df.dropna()  # Rimuovi i valori mancanti
    #df = df.apply(lambda x: (x - x.mean()) / x.std())  # Normalizzazione
    #return df.to_dict(orient='records')
    return ({"message": "Risposta Funzione preprocess_data"})
